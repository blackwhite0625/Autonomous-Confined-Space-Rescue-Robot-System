一、AI 偵測強化
1. 熱區記憶地圖 (Heat Map)
目前巡邏是無記憶的，走過的地方不會記錄
可用軟體建立簡易格狀地圖，標記已掃描/未掃描區域，避免重複搜索
純軟體實作，不需額外硬體
2. 多人追蹤 (Multi-Person Tracking)
目前 detector.py 每幀獨立偵測，不追蹤同一人
加入簡易 IoU tracking 或 DeepSORT，可區分「已確認的人」vs「新發現的人」
避免對同一受困者重複回報
3. 物件偵測擴展
YOLOv8n 本身支援 80 類物件，目前只用 person
可額外偵測：backpack、handbag、cell phone（手機燈光求救）、debris 等
災區特徵物偵測能提高搜索效率，不需重新訓練模型
4. 夜視/低光增強
USB 攝影機在暗處效果差
可用軟體做 CLAHE 直方圖均衡化，大幅提升低光偵測率
僅需在 camera.py 加幾行 OpenCV 程式碼

二、音訊系統優化
5. 音訊事件分類模型
目前 audio_detector.py 是規則式（RMS + FFT）
可加入輕量 CNN 分類器（如 YAMNet）區分：哭喊、敲擊、呼吸聲、碎石聲
RPi5 的 CPU 跑得動小型音訊模型
6. 音源方向估計 (Sound Localization)
目前只知道「有聲音」，不知道方向
若加一支 USB 麥克風（雙麥），可用 TDOA 時間差估算方位
或者用單麥 + 雲台旋轉掃描，比較各角度 RMS 強度來推估方向
7. 持續語音監聽優化
目前 STT 用 Google 雲端（需網路）
可加入離線 Whisper tiny 模型作為備援，在無網路災區仍能辨識關鍵字
RPi5 8GB 跑 whisper-tiny 可行

三、導航與巡邏強化
8. SLAM 簡易建圖
目前超音波只有前方一點距離
結合雲台旋轉 + 超音波，可在每個停留點做 180° 掃描建立簡易障礙物地圖
軟體層面，用 servo 角度 + 距離值做極座標 → 直角座標轉換
9. 路徑回溯功能
記錄移動指令序列（前進 3 秒、左轉 0.6 秒…）
搜救完成後可反向回溯原路返回出發點
純軟體，用 stack 記錄即可
10. 多搜索策略
目前只有 D（定點掃描）和 E（走停掃描）
可加入：螺旋搜索、沿牆搜索（wall-following）、隨機彈射（random bounce）
不同空間結構適用不同策略

四、安全性與可靠性
11. 傾倒偵測 (IMU)
若有 MPU6050（很便宜的 I2C 模組），可偵測機器人自身翻倒
翻倒時自動停止馬達、發送 Telegram 求援
目前硬體清單沒有，但成本極低可擴充
12. 電池電壓監控
透過 ADC 模組（如 ADS1115）監測 18650 電壓
低電量時自動返航或發出警告
避免在災區中途斷電
13. 超音波多方向偵測
目前只有前方一顆 HC-SR04
側面加裝 1-2 顆可大幅降低碰撞風險
GPIO 腳位仍有剩餘可用

五、使用者體驗 (Web UI)
14. 事件時間軸回放
event_logger.py 已儲存截圖和事件資料
Web UI 加入時間軸，可回放整個搜救過程
對事後分析和報告非常有價值
15. 即時標註疊加
目前 /video_feed 是原始影像
可將 YOLOv8 偵測框、姿態骨架、VictimScore 直接疊加在串流上
detector.py 已有 annotated frame，只需切換串流來源
16. 地圖視覺化
結合路徑記錄 + 事件位置，在 Web UI 上畫出搜索軌跡
標記發現受困者的位置
用 Canvas 或簡單 SVG 即可實現
17. 多機器人協同介面
如果未來有多台機器人，Web UI 可擴展為多機監控
目前架構（Flask REST API）已經支援這種擴展

六、通訊強化
18. 離線模式
災區可能沒有 WiFi/網路
可加入 AP 模式（RPi 自建熱點），手機直連控制
Telegram 改為本地 log 優先、有網路時批次上傳
19. 雙向語音對講
已有 USB 麥克風 + 喇叭
加入 WebRTC 或簡易音訊串流，讓操作員可以直接與受困者對話
對搜救效率有極大提升